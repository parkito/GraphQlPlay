input PageQuery {
    pageNumber: Int!
    pageSize: Int!
    sort: String
}

type PagedContent {
    pageSize: Int
    pageNumber: Int
    isFirstPage: Boolean
    isLastPage: Boolean
    totalElements: Long
    totalPages: Int
    content: [Object]
}

